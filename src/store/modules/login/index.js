import actions from './actions'
import mutations from './mutations'

const state = {
    isLogin: false,
    permission: null,
    menuPermission: null
}

export default {
    state,
    actions,
    mutations
}
